# ============================================
# Production Environment Configuration
# ============================================
# Copy this file to .env.production and fill in your values

# Application
NODE_ENV=production
PORT=3000
API_PREFIX=v1

# MongoDB Atlas Connection String
# Get this from MongoDB Atlas Dashboard -> Connect -> Drivers
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/tech-journal?retryWrites=true&w=majority

# JWT Configuration
# IMPORTANT: Generate a secure random string for production!
# Use: openssl rand -base64 64
JWT_SECRET=your-super-secure-jwt-secret-change-this
JWT_EXPIRES_IN=86400

# Admin credentials for seeding
ADMIN_EMAIL=admin@yourdomain.com
ADMIN_PASSWORD=your-secure-admin-password

# Frontend API URL (for docker-compose build)
VITE_API_BASE_URL=http://your-server-ip:3000/v1
